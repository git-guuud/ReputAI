out/
assets/
cache/
broadcast/
node_modules/
.env
docs/
.env.*
!.env.example
docs/findings/*.local.md

# Fork-test scratch output (01/02/03 scripts run against a Sepolia fork).
deployments/fork-test.json
deployments/fork-demo.json

# Live dress-rehearsal record (label `agent-rehearsal`, revoked). Scratch, not the submission.
deployments/demo-rehearsal.json

# Control-room CLI deps and its cross-pane transaction feed.
cli/node_modules/
deployments/.control-room.jsonl
deployments/demo-sepolia.json
